include ':app'
include ':capacitor-cordova-android-plugins'
project(':capacitor-cordova-android-plugins').projectDir = new File('./capacitor-cordova-android-plugins/')

apply from: 'capacitor.settings.gradle'

// Ensure :capacitor-android always points to the local vendor module for standalone builds without node_modules
if (file('./capacitor-android').exists()) {
    project(':capacitor-android').projectDir = new File('./capacitor-android')
}